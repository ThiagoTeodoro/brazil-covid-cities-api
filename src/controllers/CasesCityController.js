module.exports= {

    /**
     * Controlador para obter os dados de casos por cidade.
     * 
     * @param {*} request 
     * @param {*} response 
     */
    data(request, response){
        return response.json({status:"OK"});
    }



}